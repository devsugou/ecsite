spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/ecsite_db
    driverClassName: org.postgresql.Driver
    username: postgres
    password: mysecretpassword
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
